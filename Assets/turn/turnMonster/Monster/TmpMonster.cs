using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TmpMonster : TurnMonster
{
    // Start is called before the first frame update
    new void Awake()
    {
        base.Awake();
    }
}
