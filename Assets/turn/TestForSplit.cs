using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestForSplit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //브랜치인지 브런치인지 나뉘는지 확인용파일
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
